{
  "name": "@menu-admin-embed-sdk/restaurant-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5174 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4174 --strictPort",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@menu-admin-embed-sdk/sdk": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^8.1.5"
  }
}
